<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!--定义日志文件的存储地址 勿在 LogBack的配置中使用相对路径
    -->
    <property name="LOG_HOME" value="D:/work/Android/code/garage-log" />

    <!-- 控制台输出 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{30} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 按照每天生成日志文件 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>${LOG_HOME}/logs/garage.log.%d{yyyy-MM-dd}.log</FileNamePattern>
            <!--日志文件保留天数 -->
            <MaxHistory>30</MaxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符 -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{30} - %msg%n</pattern>
        </encoder>
        <!--日志文件最大的大小 -->
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>10MB</MaxFileSize>
        </triggeringPolicy>
    </appender>

    <!-- 日志输出级别 -->
    <root level="INFO">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="FILE" />
    </root>

    <!-- 定义各个包的详细路径，继承root的值
    <logger name="com.hry.spring" level="TRACE" />
    <logger name="com.wangyu.garage" level="debug" />
    <logger name="com.wangyu.prm" level="debug" />
    -->

    <!-- 此值由 application.properties的spring.profiles.active=dev指定
    <springProfile name="dev">
        <property name="LOG_HOME" value="D:/work/Android/code/garage-log1/" />
        <logger name="com.wangyu.garage" level="DEBUG" />
    </springProfile>

    <springProfile name="pro">
        <property name="LOG_HOME" value="/garage-log" />
        <logger name="com.wangyu.garage" level="INFO" />
    </springProfile>
    -->

</configuration>

        <!--
<?xml version='1.0' encoding='utf-8'?>
<configuration>
<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
<layout class="ch.qos.logback.classic.PatternLayout">
    <Pattern>
        %d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
        %d{yyyy-MM-dd HH:mm:ss} [%thread] %5p %c{5}:%L - %m%n
    </Pattern>
</layout>
</appender>

<logger name="com.wangyu.garage" level="debug"/>

<root level="info">
<appender-ref ref="STDOUT"/>
</root>
</configuration>
        -->
